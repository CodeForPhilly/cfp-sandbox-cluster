apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pr-100
  namespace: code-for-philly
spec:
  hostnames:
    - pr-100.codeforphilly.sandbox.k8s.phl.io
  parentRefs:
    - name: main-gateway
      namespace: envoy-gateway-system
  rules:
    - backendRefs:
        - name: pr-100-site
          port: 80
