apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pr-80
  namespace: code-for-philly
spec:
  parentRefs:
    - name: main-gateway
      namespace: envoy-gateway-system
  hostnames:
    - pr-80.codeforphilly.sandbox.k8s.phl.io
  rules:
    - backendRefs:
        - name: pr-80-site
          port: 80
