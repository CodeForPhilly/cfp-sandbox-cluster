apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: metabase
  namespace: metabase
spec:
  parentRefs:
    - name: main-gateway
      namespace: envoy-gateway-system
  hostnames:
    - metabase.sandbox.k8s.phl.io
  rules:
    - backendRefs:
        - name: metabase
          port: 3000
