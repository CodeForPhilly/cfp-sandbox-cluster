apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pr-220
  namespace: laddr
spec:
  parentRefs:
    - name: main-gateway
      namespace: envoy-gateway-system
  hostnames:
    - pr-220.laddr.sandbox.k8s.phl.io
  rules:
    - backendRefs:
        - name: pr-220-site
          port: 80
