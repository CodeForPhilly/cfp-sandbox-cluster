apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: metabase
  namespace: metabase
spec:
  hostnames:
    - metabase.sandbox.k8s.phl.io
  parentRefs:
    - name: main-gateway
      namespace: envoy-gateway-system
    - name: metabase
  rules:
    - backendRefs:
        - name: metabase
          port: 3000
