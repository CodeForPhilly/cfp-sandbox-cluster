apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: grafana
spec:
  hostnames:
    - metrics.sandbox.k8s.phl.io
  parentRefs:
    - name: grafana
  rules:
    - backendRefs:
        - name: grafana
          port: 80
