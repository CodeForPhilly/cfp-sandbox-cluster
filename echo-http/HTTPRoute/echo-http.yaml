apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: echo-http
  namespace: echo-http
spec:
  hostnames:
    - echo-http.sandbox.k8s.phl.io
  parentRefs:
    - name: main-gateway
      namespace: envoy-gateway-system
    - name: echo-http
  rules:
    - backendRefs:
        - name: echo-http
          port: 80
