apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: balancer
  namespace: balancer
spec:
  hostnames:
    - balancer.sandbox.k8s.phl.io
  parentRefs:
    - name: balancer
  rules:
    - backendRefs:
        - name: balancer
          port: 80
